import { WarnRemarkData } from '@/services/riskControlAlarm';
import React, { useEffect } from 'react';
import { colorSpan, warnValue, showWarnType } from './until';

interface Props {
  data?: WarnRemarkData;
}

const J4: React.FC<Props> = ({ data }) => {
  // useEffect(() => {}, [data?]);
  return <></>;
};

export default J4;
