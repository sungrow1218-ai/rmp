import React from 'react';

interface Props {
  marketId?: number;
}

const Z06202: React.FC<Props> = ({ marketId }) => {
  // useEffect(() => {}, [data]);
  return <>未找到对应规则类型的监督规则</>;
};

export default Z06202;
